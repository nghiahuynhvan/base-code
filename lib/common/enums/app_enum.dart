// ignore_for_file: public_member_api_docs

/// This file contains different enums.
/// This class is used to define an enumeration type in project
/// to store finite data members under the same type definition.
enum AppState { idle, loading, done, error }

enum ConnectionType { wifi, mobile, ethernet }

enum ThemeType { dark, light }

enum NetworkAlertType { none, toast, dialog }